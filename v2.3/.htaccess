<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    
    # Usuñ .php z URLs (opcjonalnie)
    # RewriteCond %{REQUEST_FILENAME} !-f
    # RewriteCond %{REQUEST_FILENAME} !-d
    # RewriteRule ^([^\.]+)$ $1.php [NC, L]
    
    # Wyj¹tek dla order_success.php - nie przekierowuj tego pliku
    RewriteCond %{REQUEST_URI} !^/order_success\.php
    
    # Jêzyk z query parameter
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php?%{QUERY_STRING} [QSA,L]
</IfModule>

# Bezpieczeñstwo
<FilesMatch "\.(?:env|sql|md|psd)$">
    Deny from all
</FilesMatch>

# Kompresja
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript
</IfModule>

# Cache Control
<IfModule mod_headers.c>
    <FilesMatch "\.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2)$">
        Header set Cache-Control "max-age=31536000, public"
    </FilesMatch>
    
    <FilesMatch "\.(html|htm|php)$">
        Header set Cache-Control "max-age=3600, must-revalidate"
    </FilesMatch>
</IfModule>

# HTTPS redirect (opcjonalnie - odkomentuj jeœli masz SSL)
# <IfModule mod_rewrite.c>
#     RewriteCond %{HTTPS} off
#     RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
# </IfModule>
